#include "../headers/cube.h"

Cube::Cube(const glm::vec3 &position, const glm::vec3 &rotation, const glm::vec3 &size, const glm::vec3 &color)
	: position(position), rotation(rotation), size(size), color(color) {}